const  logToUpperCase = (msg) => {
    console.log('Logger message: ' + msg.toUpperCase());
};

const logToLowerCase = (msg) => {
    console.log('Logger message: ' + msg.toLowerCase());
};

export {logToLowerCase, logToUpperCase};